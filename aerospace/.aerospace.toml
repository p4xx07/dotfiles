# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking

# You can use it to add commands that run after login to macOS user session.
# 'start-at-login' needs to be 'true' for 'after-login-command' to work
# Available commands: https://nikitabobko.github.io/AeroSpace/commands
after-login-command = []
# You can use it to add commands that run after AeroSpace startup.
# 'after-startup-command' is run after 'after-login-command'
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
after-startup-command = [
  'exec-and-forget borders active_color=0xffe1e3e4 inactive_color=0xff494d64 width=5.0',
]

# Start AeroSpace at login
start-at-login = false

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 30

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# Fallback value (if you omit the key): on-focused-monitor-changed = []
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# You can effectively turn off macOS "Hide application" (cmd-h) feature by toggling this flag
# Useful if you don't use this macOS feature, but accidentally hit cmd-h or cmd-alt-h key
# Also see: https://nikitabobko.github.io/AeroSpace/goodies#disable-hide-app
automatically-unhide-macos-hidden-apps = false

# Possible values: (qwerty|dvorak|colemak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
[key-mapping]
    preset = 'qwerty'

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See:
#                 https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
inner.horizontal = 5
inner.vertical = 5
outer.left = 15
outer.bottom = 15
outer.top = 15
outer.right = 15

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
# Fallback value (if you omit the key): mode.main.binding = {}
[mode.main.binding]

    # All possible keys:
    # - Letters.        a, b, c, ..., z
    # - Numbers.        0, 1, 2, ..., 9
    # - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
    # - F-keys.         f1, f2, ..., f20
    # - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon,
    #                   backtick, leftSquareBracket, rightSquareBracket, space, enter, esc,
    #                   backspace, tab, pageUp, pageDown, home, end, forwardDelete,
    #                   sectionSign (ISO keyboards only, european keyboards only)
    # - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
    #                   keypadMinus, keypadMultiply, keypadPlus
    # - Arrows.         left, down, up, right

    # All possible modifiers: cmd, alt, ctrl, cmd-alt

    # All possible commands: https://nikitabobko.github.io/AeroSpace/commands

    # See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
    # You can uncomment the following lines to open up terminal with alt + enter shortcut
    # (like in i3)
    # cmd-alt-enter = '''exec-and-forget osascript -e '
    # tell application "Terminal"
    #     do script
    #     activate
    # end tell'
    # '''

    # See: https://nikitabobko.github.io/AeroSpace/commands#layout
    ctrl-cmd-alt-slash = 'layout tiles horizontal vertical'
    ctrl-cmd-alt-comma = 'layout accordion horizontal vertical'

    # See: https://nikitabobko.github.io/AeroSpace/commands#focus
    ctrl-cmd-alt-h = 'focus left'
    ctrl-cmd-alt-j = 'focus down'
    ctrl-cmd-alt-k = 'focus up'
    ctrl-cmd-alt-l = 'focus right'

    # See: https://nikitabobko.github.io/AeroSpace/commands#move
    ctrl-cmd-shift-alt-h = 'move left'
    ctrl-cmd-shift-alt-j = 'move down'
    ctrl-cmd-shift-alt-k = 'move up'
    ctrl-cmd-shift-alt-l = 'move right'

    # See: https://nikitabobko.github.io/AeroSpace/commands#resize
    ctrl-cmd-alt-minus = 'resize smart -50'
    ctrl-cmd-alt-equal = 'resize smart +50'

    # See: https://nikitabobko.github.io/AeroSpace/commands#workspace
    ctrl-cmd-alt-0 = 'workspace 0'
    ctrl-cmd-alt-1 = 'workspace 1'
    ctrl-cmd-alt-2 = 'workspace 2'
    ctrl-cmd-alt-3 = 'workspace 3'
    ctrl-cmd-alt-4 = 'workspace 4'
    #alt-5 = 'workspace 5' for ~
    #alt-6 = 'workspace 6'
    #alt-7 = 'workspace 7'
    #alt-8 = 'workspace 8'
    #alt-9 = 'workspace 9' for `
    ctrl-cmd-alt-a = 'workspace A' # In your config, you can drop workspace bindings that you don't need
    ctrl-cmd-alt-b = 'workspace B'
    ctrl-cmd-alt-c = 'workspace C'
    ctrl-cmd-alt-d = 'workspace D'
    ctrl-cmd-alt-e = 'workspace E'
    ctrl-cmd-alt-f = 'workspace F'
    ctrl-cmd-alt-g = 'workspace G'
    ctrl-cmd-alt-i = 'workspace I'
    ctrl-cmd-alt-m = 'workspace M'
    ctrl-cmd-alt-n = 'workspace N'
    ctrl-cmd-alt-o = 'workspace O'
    ctrl-cmd-alt-p = 'workspace P'
    ctrl-cmd-alt-q = 'workspace Q'
    ctrl-cmd-alt-r = 'workspace R'
    ctrl-cmd-alt-s = 'workspace S'
    ctrl-cmd-alt-t = 'workspace T'
    ctrl-cmd-alt-u = 'workspace U'
    ctrl-cmd-alt-v = 'workspace V'
    ctrl-cmd-alt-w = 'workspace W'
    ctrl-cmd-alt-x = 'workspace X'
    ctrl-cmd-alt-y = 'workspace Y'
    ctrl-cmd-alt-z = 'workspace Z'

    # See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
    ctrl-cmd-shift-alt-1 = 'move-node-to-workspace 1'
    ctrl-cmd-shift-alt-2 = 'move-node-to-workspace 2'
    ctrl-cmd-shift-alt-3 = 'move-node-to-workspace 3'
    ctrl-cmd-shift-alt-4 = 'move-node-to-workspace 4'
    ctrl-cmd-shift-alt-5 = 'move-node-to-workspace 5'
    ctrl-cmd-shift-alt-6 = 'move-node-to-workspace 6'
    ctrl-cmd-shift-alt-7 = 'move-node-to-workspace 7'
    ctrl-cmd-shift-alt-8 = 'move-node-to-workspace 8'
    ctrl-cmd-shift-alt-9 = 'move-node-to-workspace 9'
    ctrl-cmd-shift-alt-a = 'move-node-to-workspace A'
    ctrl-cmd-shift-alt-b = 'move-node-to-workspace B'
    ctrl-cmd-shift-alt-c = 'move-node-to-workspace C'
    ctrl-cmd-shift-alt-d = 'move-node-to-workspace D'
    ctrl-cmd-shift-alt-e = 'move-node-to-workspace E'
    ctrl-cmd-shift-alt-f = 'move-node-to-workspace F'
    ctrl-cmd-shift-alt-g = 'move-node-to-workspace G'
    ctrl-cmd-shift-alt-i = 'move-node-to-workspace I'
    ctrl-cmd-shift-alt-m = 'move-node-to-workspace M'
    ctrl-cmd-shift-alt-n = 'move-node-to-workspace N'
    ctrl-cmd-shift-alt-o = 'move-node-to-workspace O'
    ctrl-cmd-shift-alt-p = 'move-node-to-workspace P'
    ctrl-cmd-shift-alt-q = 'move-node-to-workspace Q'
    ctrl-cmd-shift-alt-r = 'move-node-to-workspace R'
    ctrl-cmd-shift-alt-s = 'move-node-to-workspace S'
    ctrl-cmd-shift-alt-t = 'move-node-to-workspace T'
    ctrl-cmd-shift-alt-u = 'move-node-to-workspace U'
    ctrl-cmd-shift-alt-v = 'move-node-to-workspace V'
    ctrl-cmd-shift-alt-w = 'move-node-to-workspace W'
    ctrl-cmd-shift-alt-x = 'move-node-to-workspace X'
    ctrl-cmd-shift-alt-y = 'move-node-to-workspace Y'
    ctrl-cmd-shift-alt-z = 'move-node-to-workspace Z'

    # See: https://nikitabobko.github.io/AeroSpace/commands#workspace-back-and-forth
    ctrl-cmd-alt-tab = 'workspace-back-and-forth'
    # See: https://nikitabobko.github.io/AeroSpace/commands#move-workspace-to-monitor
    # shift-alt-tab = 'move-workspace-to-monitor --wrap-around next'

    # See: https://nikitabobko.github.io/AeroSpace/commands#mode
    ctrl-cmd-alt-period = 'mode service'

# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
    esc = ['reload-config', 'mode main']
    r = ['flatten-workspace-tree', 'mode main'] # reset layout
    f = ['layout floating tiling', 'mode main']
    backspace = ['close-all-windows-but-current', 'mode main']

    # sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/1
    #s = ['layout sticky tiling', 'mode main']

    alt-cmd-alt-h = ['join-with left', 'mode main']
    alt-cmd-alt-j = ['join-with down', 'mode main']
    alt-cmd-alt-k = ['join-with up', 'mode main']
    alt-cmd-alt-l = ['join-with right', 'mode main', 'exec-and-forget sketchybar --trigger aerospace_leave_service_mode']

    down = 'volume down'
    up = 'volume up'
    cmd-alt-down = ['volume set 0', 'mode main']

[[on-window-detected]]
    if.app-id = 'com.usebruno.app'
    run = 'move-node-to-workspace P'

[[on-window-detected]]
    if.app-id = 'org.jkiss.dbeaver.core.product'
    run = 'move-node-to-workspace D'
		
[[on-window-detected]]
    if.app-id = 'ru.keepcoder.Telegram'
    run = 'move-node-to-workspace Q'

[[on-window-detected]]
    if.app-id = 'net.whatsapp.WhatsApp'
    run = 'move-node-to-workspace W'

[[on-window-detected]]
    if.app-id = 'com.microsoft.teams2'
    run = 'move-node-to-workspace V'

[[on-window-detected]]
	if.app-id = 'app.zen-browser.zen'
	run = 'move-node-to-workspace B'

[[on-window-detected]]
    if.app-id = 'com.mitchellh.ghostty'
    run = 'move-node-to-workspace T'

[[on-window-detected]]
	if.app-id = 'com.microsoft.Outlook'
    run = 'move-node-to-workspace O'

[[on-window-detected]]
	if.app-id = 'net.ankiweb.launcher'
    run = 'move-node-to-workspace A'

[[on-window-detected]]
	if.app-id = 'com.google.Chrome.app.kjbdgfilnfhdoflbpgamdcdgpehopbe'
    run = 'move-node-to-workspace C'

[[on-window-detected]]
	if.app-id = 'com.todoist.mac.Todoist'
    run = 'move-node-to-workspace 0'

[[on-window-detected]]
	if.app-id = 'org.mozilla.firefox'
    run = 'move-node-to-workspace F'

[[on-window-detected]]
	if.app-id = 'com.google.Chrome'
    run = 'move-node-to-workspace B'

[[on-window-detected]]
  if.app-id = 'com.google.Chrome.app.mnhkaebcjjhencmpkapnbdaogjamfbcj'
    run = 'move-node-to-workspace M'

[[on-window-detected]]
    if.app-id = 'org.jeffvli.feishin'
    run = 'move-node-to-workspace S'

[workspace-to-monitor-force-assignment]
    T = 'main' 	    # Terminal
    B = 'main' 	    # Browser
    O = 'main' 	    # Outlook
    W = 'secondary' # WhatsApp
    Q = 'secondary' # Telegram
    V = 'secondary' # Teams
    F = 'secondary' # Firefox / other browsers

    P = 3
